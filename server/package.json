{
  "name": "shopping_list-server",
  "version": "0.1.0",
  "description": "Shopping List server (Express)",
  "license": "License Commercial",
  "dependencies": {
    "ajv": "^8.12.0",
    "express": "^5.1.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=16.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "dev": "node app.js",
    "test": "echo \"No tests configured\" && exit 0",
    "clean": "echo \"no clean step defined\"",
  "build": "powershell -NoProfile -Command \"$items=@(); foreach($p in @('app','app.js','env','public','package.json','README.md')) { if(Test-Path $p){ $items += $p }}; if($items.Count -eq 0){ Write-Error 'No files to archive'; exit 1 }; if(-Not (Test-Path -Path build)){New-Item -ItemType Directory -Path build | Out-Null}; Compress-Archive -Path $items -DestinationPath 'build/server-build.zip' -Force; Write-Host 'Created build/server-build.zip'\""
  },
  "main": "app.js",
  "files": [
    "app",
    "env",
    "public"
  ],
  "prettier": {
    "printWidth": 120
  }
}
